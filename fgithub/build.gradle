plugins {
    id "org.junit.platform.gradle.plugin"
}

dependencies {
    compile project(":core")
    compile project(":git")

    // Github client
    compile "org.kohsuke:github-api:$kohsuke_github_version"
}